{
  "buildCommand": "npm install && cd apps/client && npx vite build",
  "outputDirectory": "apps/client/dist",
  "installCommand": "echo 'skipped'",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://pulsebiserver-production.up.railway.app/api/:path*"
    }
  ]
}
